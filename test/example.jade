doctype 5
html(lang="en" data-type="test")

  head
    meta(charset="utf-8")
    title Hello

  {block #main}
    body
      {if $cond}
        h1 title
      {else}
        h1 Hi
      p. inline text
        another text
      - $x = 1
      = $x
        . 'multi'
        . 'line'
      != $x

      //
        block
        <comment>
      #id.class.
        unformated
        | &lt;text&gt;
